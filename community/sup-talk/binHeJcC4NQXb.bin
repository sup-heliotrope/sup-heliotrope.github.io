# Before add message script 

# Add labels to mailing list i am subscribed

# Sup talk
if message.subj =~ /\[sup-talk\]/i
  message.add_label "sup-talk"
end 

# Arch general
if message.subj =~ /\[arch-general\]/i
  message.add_label "arch-general"
end

# Doxygen users
if message.subj =~ /\[doxygen-users\]/i
  message.add_label "doxygen-users"
end

# Paparazzi-devel
if message.subj =~ /\[paparazzi-devel\]/i
  message.add_label "paparazzi-devel"
end

# Latex
if message.subj =~ /\[texhax\]/i
  message.add_label = "texhax"
end
